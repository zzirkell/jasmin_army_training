# Pro 04: Constants, Global, Final

Use `Final` constants like `HEART_COUNT` and `DEFAULT_COW_SPEED`. Explain that `Final` is a hint.

in java final is an actual constraint so if you say that variable is final, you would not be able to change its value. but in python this is just a hint. so, if you know that number of hearts of person is 1, make this variable final (will not change to 2 anyhow) and global (because it has the same value for each and every human). you can look up those concepts in the internet, they are all just OOP concepts