# Exercise 09: Summer Animal Camp

## Goal
Create a second organizer class, similar to Farm, but for a summer camp.

## Methods

```python
add_animal(animal)
morning_concert()
morning_run()
find_fast_animals(minimum_speed)
camp_report()
```

Use polymorphism: call `animal.make_sound()` and `animal.move()` without caring which animal class it is.
