# Exercise 05: Inheritance with Animal

## Goal
Create parent class `Animal` because Cow and Sheep repeat attributes and methods. We are trying to make a hierarchy because why not? this is just an OOP concept

## Animal stores
`name`, `eyes`, `legs`, `speed`, `sound`.

## Animal methods
`run()`, `make_sound()`, `describe()`.

Use `super().__init__(...)` in Cow and Sheep.
