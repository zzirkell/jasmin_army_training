# Exercise 06: Abstract Animal

## Goal
Make `Animal` abstract. Because animals are abstract what can i say but cows are real!! Basically, you cannot create an object Animal by making its class abstract. but you can still create Cow. makes a lot of sense, right?

Use:

```python
from abc import ABC, abstractmethod
```

Make `make_sound()` abstract so every real animal must implement it.
