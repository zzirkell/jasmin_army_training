# Exercise 08: Movement Contract

## Goal
Introduce the interface idea.

Python has no Java-style interfaces. Here we use an abstract class `Movable` with required method `move()`.

Everything movable can be used like this:

```python
thing.move()
```
